Gonna provide a ps1 file
load the ps1 file in ise
gonna give us a bunch of functions in the ise
put command within function to get result
save ps1 and run executable


"return" will get atleast 5 points

return $var1+$var2+$var3+$var4

$content = get-content $file
foreach ($item in $content) {
    if ( $line.startwith($start) ) {
        return $line
    }
    return $null
}

function q($arr) {
  return $arr -join('/') 
}

function q() {
return get-process | sort-object -property Processname -descending
}

function q() {
        foreach ($i in $arr) {
            if ( $i[0] -eq $key ) {
                return $i[9]
            }
        return -1
        }
}

Advance functions, provided on a pipeline
function add-nums {
    
    Begin {
        $sum=0
    }
Process {
    <Command>
   }
   End {
   $sum
   }
}

just need Process blocks

Procss {
    $sum = $var1 + $var2
    }
